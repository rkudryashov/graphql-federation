type Query {
    # todo by planet
    satellites: [Satellite!]!
    satellite(id: ID!): Satellite!
}

type Satellite {
    id: ID!
    name: String!
}

type Planet @key(fields: "id") @extends {
    id: ID! @external
    satellites: [Satellite!]
}
